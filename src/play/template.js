import { dContainer } from '../asprite';

export default function Template(play, ctx) {

  const { textures } = ctx;

  this.init = data => {
  };
  
  this.update = delta => {
  };

  const container = this.container = dContainer();

  const initContainer = () => {
  };
  initContainer();

  this.render = () => {
  };

}
