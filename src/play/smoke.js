import { objMap } from '../util2';
import { pContainer, dContainer, sprite, tsprite, asprite } from '../asprite';

import * as v from '../vec2';

import Pool from 'poolf';

export default function Smokes(play, ctx, worms) {

  const { textures } = ctx;

  let smokes = [];

  this.init = (data) => {

  };
  
  this.update = delta => {
  };

  this.render = () => {
  };

}
